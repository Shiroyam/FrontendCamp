{
  "name": "root",
  "private": true,
  "scripts": {
    "start:lerna": "lerna run start --parallel",
    "build:lerna": "lerna run build",
    "lint:lerna": "lerna run lint",
    "format:lerna": "lerna run format",
    "stylelint:lerna": "lerna run stylelint",
    "test:lerna": "lerna run test --parallel",
    "storybook:build": "lerna run storybook:build",
    "storybook:lerna": "lerna run storybook",
    "preparev": "husky install"
  },
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "devDependencies": {
    "husky": "^8.0.3",
    "lerna": "^6.6.2",
    "lint-staged": "^13.2.2"
  }
}
